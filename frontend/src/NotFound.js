import React from 'react'
import {Container, Header,} from 'semantic-ui-react'

const NotFound = () => (
    <Container style={{marginTop: '7em'}}>
        <Header as='h1'>Page was not found...</Header>
    </Container>
)

export default NotFound